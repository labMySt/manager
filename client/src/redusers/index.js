import {
  AUTH_USER,
  AUTH_USER_FATCHED,
  AUTH_USER_ERROR
} from '../actions/types';

const initialState = {};

export default function reduser(state = initialState, action) {
  switch(action.type) {
    default: return state;
  }

};
